export class RoadManager {
  constructor(scene) {
    this.scene = scene;
    this.roads = [];
  }

  // Phase 2 placeholder.
  // Road construction, snapping and intersections will be implemented next.
}
